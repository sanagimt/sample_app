<h2>タイトル</h2>
<p><%= @list.title %></p>

<h2>本文</h2>
<p><%= @list.body %></p>

<h2>画像</h2>
<!-- validation追加にてif削除。画像が存在しない場合の処理が不要のため、以下追加 -->
<%= image_tag @list.image, size: "300x300" %>

<p></p>
<%= link_to "編集", edit_list_path(@list.id) %>
<%= link_to "削除", list_path(@list.id), method: :delete, "data-confirm" => "本当に削除しますか？" %>
<!-- destroyアクションのHTTPメソッドはdelete -->
<!-- deta-confirmで削除確認ダイアログを表示 -->
